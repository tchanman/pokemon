# Pokemon
It's pokemon (sorta) made with python

# TODO
 - make backgrounds for all levels
 - make start screen



if hit:
    for box in walls:
        if inbox(tl,box) and inbox(tr,box):
            canmove_b is false
